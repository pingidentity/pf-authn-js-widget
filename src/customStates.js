export function getCustomStates(dispatch) {
  return {
    /**
     * add additional states for custom API enabled plugins
     * state_name: function() event initializer
     * for each state, include a state_name(lowercase).handlebars template file under /partials dir
     */
  }
}
